# Ignore node modules
node_modules
services/*/node_modules
packages/*/node_modules

# Git
.git
.gitignore

# Local env
.env
*.log

# Keep deployment and CI files (cloudbuild.yaml, scripts, Dockerfiles)
# Do not ignore .sh or .yml globally so Cloud Build can find cloudbuild.yaml